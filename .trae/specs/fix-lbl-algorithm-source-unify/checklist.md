- [x] Footer `effectiveAlgorithm` 在 LBL 模式下使用 `currentChildAlgorithm`，Normal 模式下使用 `algorithmFromSession`
- [x] Footer space 键使用 `effectiveAlgorithm` 决定 `intervalPractice()` vs `gradeFn(5)`
- [x] Footer F/H/G 快捷键使用 `effectiveAlgorithm` 决定启用/禁用
- [x] Footer E 快捷键使用 `effectiveAlgorithm` 决定启用/禁用
- [x] Footer GradingControlsWrapper `isAutoAdvanceMode` 使用 `effectiveAlgorithm`
- [x] PracticeOverlay showAnswers useEffect：childSessionData 为空时保持 showAnswers=true
- [x] PracticeOverlay Footer showAnswers prop 直接传递 `showAnswers` state（无 prop override）
- [x] PracticeOverlay Footer setShowAnswers prop 始终传递 `setShowAnswers`
- [x] LBL + Progressive 子 block：不显示 Show Answer，直接显示 Read+Next
- [x] LBL + SM2 子 block：显示 Show Answer，评分后推进
- [x] LBL + Progressive 子 block：space 键触发 Next（不是 Perfect）
- [x] LBL + SM2 子 block：space 键触发 Perfect（答案已显示时）
- [x] lint 和 typecheck 通过
