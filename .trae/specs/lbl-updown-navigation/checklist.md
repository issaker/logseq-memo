- [x] `useLineByLineReview` 新增 `onLineByLinePrev` 回调：索引 > 0 时减 1，确保 revealedCount 覆盖目标位置
- [x] `useLineByLineReview` 新增 `onLineByLineNext` 回调：索引 < length-1 时加 1，确保 revealedCount 覆盖目标位置
- [x] `lineByLineIsCardComplete` 在用户回退时变为 false，恢复评分能力
- [x] `onLineByLineGrade` 评分后正确推进到下一个到期子 block，完成状态判断正确
- [x] 回溯重评分场景：`savePracticeData` 同日覆盖正确触发
- [x] 回溯重评分场景：`childSessionData` 和 `sessionOverrides` 即时更新
- [x] 回溯重评分场景：`updateParentNextDueDate` 正确调用
- [x] 回溯重评分与 Forgot 重插入机制兼容
- [x] 回溯重评分与 LblNext 重插入机制兼容
- [x] `PracticeOverlay` 将 `onLineByLinePrev` 和 `onLineByLineNext` 传入 `MainContext`
- [x] `PracticeOverlay` 的 `setShowAnswers` 逻辑在翻行后正确设置（已掌握→true，SM2未评分→false，LblNext→true）
- [x] `PracticeOverlay` 的 `onPracticeClick` 在回退后评分走正常 LBL 流程
- [x] `Footer` `GradingControlsWrapper` 中 LBL 模式下显示 ▲/▼ 按钮
- [x] `Footer` `LblCompletedControls` 中显示 ▲/▼ 按钮（▲可点击，▼不可点击）
- [x] ▲/▼ 按钮样式与 ◀/▶ 按钮一致
- [x] ↑ 快捷键在 LBL 模式下触发 `onLineByLinePrev`
- [x] ↓ 快捷键在 LBL 模式下触发 `onLineByLineNext`
- [x] ↑/↓ 快捷键在 Normal 模式下无特殊行为
- [x] `LineByLineView` 当前行高亮随浏览位置正确更新
- [x] `LineByLineView` 行号指示器随浏览位置更新
- [x] lint 和 typecheck 通过
